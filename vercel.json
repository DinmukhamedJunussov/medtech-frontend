{
  "buildCommand": "pnpm install --no-frozen-lockfile --shamefully-hoist && pnpm build",
  "installCommand": "pnpm install --no-frozen-lockfile --shamefully-hoist",
  "framework": "nextjs",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ]
} 